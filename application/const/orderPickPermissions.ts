export enum OrderPickPermissionEnum {
  OR_PICK = 'or_pick',
  OR_PICK_ASSET = 'asset_pick',
  OR_PICK_INV = 'inventory_pick',
}

export enum PickOrderItemTypeEnum {
  ASSET = 'asset',
  INVENTORY = 'inventory',
}
